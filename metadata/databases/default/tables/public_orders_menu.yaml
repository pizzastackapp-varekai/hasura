table:
  name: orders_menu
  schema: public
object_relationships:
  - name: item
    using:
      manual_configuration:
        column_mapping:
          menu_id: id
        insertion_order: null
        remote_table:
          name: menu
          schema: public
